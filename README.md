# PlanarPlugins

A monorepo for tiny one-off plugins.  
These are largely created for friends, so it is unlikely that any actual releases will be cut.

# Building

To build every plugin, perform a normal build: `./gradlew build`.  
To build a specific plugin, run the build task for its module: `./gradlew :fortuneshears:build`
